<%= semantic_form_for @unit, :html => { :class => 'form-horizontal' } do |f| %>
		<%= f.inputs do %>
				<%= f.input :name %>
		<% end %>
		<%= f.actions do %>
			<%= f.action :submit %>
			<%= f.action :cancel %>
		<% end %>
<% end %>
