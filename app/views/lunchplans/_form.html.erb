<%= form_for(@lunchplan) do |f| %>
  <% if @lunchplan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lunchplan.errors.count, "error") %> prohibited this lunchplan from being saved:</h2>

      <ul>
      <% @lunchplan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day %><br />
    <%= f.date_select :day %>
  </div>
  <div class="field">
    <%= f.label :recipe_id %><br />
    <%= f.number_field :recipe_id %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
