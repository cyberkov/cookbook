<%= semantic_form_for @lunchplan, :html => { :class => 'form-horizontal' } do |f| %>
		<%= f.inputs do %>
				<%= f.input :day %>
				<%= f.input :recipe %>
				<%= f.input :notes %>
		<% end %>
		<%= f.actions do %>
			<%= f.action :submit %>
			<%= f.action :cancel %>
		<% end %>
<% end %>
